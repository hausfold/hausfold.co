---
title: Choose a desktop
description: haus supplies rooms; a desktop curates them; your host file makes one yours. The four that ship, side by side, and how to switch.
icon: choose
related:
  - title: "Start blank"
    description: "The from-scratch desktop, and adding rooms one at a time."
    href: "/docs/haus/desktops/blank"
    icon: "blank"
  - title: "Customize a desktop"
    description: "The one file you edit, and beating anything a desktop set."
    href: "/docs/haus/desktops/customizing"
    icon: "dials"
  - title: "Create a desktop"
    description: "Write your own, and prove it before anyone else runs it."
    href: "/docs/haus/desktops/creating"
    icon: "hammer"
---

**haus supplies rooms. A desktop curates them. Your host file makes one yours.**

A **room** is one capability (the bar, the launcher, window management) with its
own options and its own switch. A **desktop** is a complete answer to *what
should this Mac feel like?*: which rooms are on, and what the visible choices
are. Your **host file** is where your name, your keys and your own overrides
live.

```text
haus foundation          the rooms, their options, safe defaults
      ↓
one desktop              hacker, everyday, minimal, blank, or yours
      ↓
your host file           identity, secrets, hardware, and any override
      ↓
`haus set`               what the palette and your agent write for you
```

Later layers win, deliberately. Anything a desktop chose, you change with a
plain assignment in your host file: no `lib.mkForce` anywhere.

## Exactly one

A Mac runs **one** desktop. They don't stack, and the build says so: selecting
two is refused whether they disagree or not, and so is selecting the same one
twice, with a message of its own. Choosing again changes one line rather than
unpicking a pile. If nothing fits,
[Start blank](/docs/haus/desktops/blank) selects nothing, which keeps "build my
own" one desktop rather than a second mode.

## The four that ship

Not a ladder where each contains the last: `minimal` keeps the developer tooling
that `everyday` deliberately drops. Read the columns.

| | [hacker](/docs/haus/desktops/hacker) | [everyday](/docs/haus/desktops/everyday) | [minimal](/docs/haus/desktops/minimal) | [blank](/docs/haus/desktops/blank) |
|---|:-:|:-:|:-:|:-:|
| **For** | keyboard-first, developer, tiled | someone who doesn't write code | the themed shell, stock macOS | building your own |
| [Development](/docs/haus/rooms/development) | ✓ | · | ✓ | · |
| [AI](/docs/haus/rooms/ai) | ✓ | · | · | · |
| [Windows](/docs/haus/rooms/windows) | ✓ | · | · | · |
| [Bar](/docs/haus/rooms/bar) | ✓ | ✓ | · | · |
| [Launcher](/docs/haus/rooms/launcher) | ✓ | ✓ | · | · |
| [Shelf](/docs/haus/rooms/shelf) | ✓ | ✓ | · | · |
| [Focus](/docs/haus/rooms/focus) | ✓ | ✓ | · | · |
| [Security](/docs/haus/rooms/security) | ✓ | ✓ | ✓ | · |
| Theme (the palette) | ✓ | ✓ | ✓ | · |
| Theme ports | ✓ | ✓ | · | · |
| Generated wallpaper | ✓ | ✓ | · | · |

## Selecting one

Your `~/.config/nix/flake.nix` names it in one line:

```nix
darwinConfigurations.lovelace = haus.mkHaus {
  username = "ada";
  hostname = "lovelace";
  host = ./hosts/lovelace;
  desktop = haus.desktops.everyday;
};
```

Then apply it:

```sh
haus rebuild
```

Leave the `desktop` line out and you get **hacker**, which is what every
machine installed before desktops existed is still running. The installer writes
the line for you, and `HAUS_DESKTOP=minimal` picks another on an [unattended
install](/docs/haus/install#unattended-installs).

<Callout title="A desktop you found online is one command, not a hand-edit">
`haus show github:ada/writer-desktop` fetches it into the store and reports what
it sets, what it leaves alone and, leaf by leaf, [what would change
here](/docs/haus/reference/haus#what-would-change-here), including the leaves
your own host file outranks so the desktop cannot move them at all. It writes
nothing in your config. `haus add github:ada/writer-desktop` repeats that
check, then pins and selects it in one edit to `flake.nix` and `flake.lock`, so
`haus rebuild` is the only step left. `haus desktop` switches and `haus remove`
unpins. [Share a desktop](/docs/haus/desktops/sharing) and the [CLI
reference](/docs/haus/reference/haus#pinning-a-desktop-you-found) have all four
commands in full.
</Callout>

## Changing your mind

Switch the line, rebuild, and `haus rollback` if you don't like it: a desktop
sets options, and a rebuild computes those from scratch every time. What a
switch does **not** touch is anything outside haus. Files you made, apps you
installed by hand and the macOS settings haus never claimed all stay, and
turning a room off removes what that room installed without tidying up after
you.
