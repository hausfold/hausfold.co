# Check the docs build: it type-checks, it lints, and — the one that isn't
# boilerplate — it produces the SAME bytes twice.
#
# Why the double build is here rather than being a note in a plan:
#
# Next mints a random build id per build and embeds it in
# `_next/static/<id>/`, in every RSC payload and in 404.html. Two builds of
# identical source therefore differ in dozens of files, including two runs a
# minute apart in the same directory. `next.config.mjs` pins
# `generateBuildId`, which makes the whole export byte-identical — and that is
# what makes "did this deploy change anything?" an answerable question at all.
#
# But reproducibility is a property of these versions of Next and Fumadocs,
# not a promise either of them makes. Without a check, the day a release
# introduces a timestamp is the day the property quietly stops holding and
# nothing says so. This costs one extra build and is the only thing that would
# notice.
#
# It also covers the search index, which is the file this repo would most
# regret being non-deterministic: `out/api/search` is a fully serialized Orama
# database, ~300 KB today and growing with the corpus.
name: Docs

on:
  pull_request:
    paths:
      - 'content/**'
      - 'src/**'
      - 'public/**'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.mjs'
      - 'postcss.config.mjs'
      - 'tsconfig.json'
      - 'eslint.config.mjs'
      - '.github/workflows/docs.yml'
  push:
    branches: [main]
    paths:
      - 'content/**'
      - 'src/**'
      - 'public/**'
      - 'package.json'
      - 'package-lock.json'
      - 'next.config.mjs'
      - 'postcss.config.mjs'
      - 'tsconfig.json'
      - 'eslint.config.mjs'
      - '.github/workflows/docs.yml'
  workflow_dispatch: {}

permissions:
  contents: read

concurrency:
  group: docs-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Type-check
        run: npm run types:check

      - name: Lint
        run: npm run lint

      # Both builds start from no Turbopack cache — the same state a deploy
      # runs in, since CI never restores `.next`.
      #
      # The first `out-first` copy lives OUTSIDE the checkout, in
      # `$RUNNER_TEMP`, and that placement is load-bearing, not tidiness.
      # `.gitignore` only excludes `/out/`, not `/out-first/` — so a same-name
      # copy left inside the repo is a new, unignored directory of rendered
      # HTML sitting next to the source when the second build runs. Tailwind
      # v4's automatic content scanner walks the whole project (skipping only
      # what `.gitignore` excludes) looking for class names to generate, and
      # it can't tell "previous build output" from "source" — so it picked up
      # every class already baked into `out-first`'s HTML as a candidate,
      # generating a CSS file dozens of classes larger than the first build's
      # and, with it, a different chunk hash and a different byte in every
      # page that references it. Measured 2026-08-12: with `out-first` inside
      # the checkout, two cold builds of identical source differed in every
      # HTML file; moved to `$RUNNER_TEMP`, six consecutive cold builds
      # produced byte-identical output, filenames included.
      - name: Build once, cold
        run: |
          rm -rf .next out
          npm run build
          mv out "$RUNNER_TEMP/out-first"

      - name: Build again, cold
        run: |
          rm -rf .next
          npm run build

      # `diff -rq` and not a hash of the tree: when this fails, the useful
      # output is WHICH files moved, because that names the thing that started
      # embedding a clock.
      #
      # ...and, since 2026-08-16, WHERE each one moved. A failure used to print
      # one line naming a file and nothing else, which for a 2 MB rendered page
      # is not enough to tell a clock from a re-ordered map from a stream that
      # flushed in a different place. The loop below prints the first differing
      # byte and the bytes either side of it, per file, which is the difference
      # between "something changed" and a diagnosis. It runs only on failure,
      # so it costs nothing on a green run.
      - name: The two builds must be byte-identical
        run: |
          if diff -rq "$RUNNER_TEMP/out-first" out > "$RUNNER_TEMP/differs.txt"; then
            echo "Two builds, identical output."
            exit 0
          fi
          cat "$RUNNER_TEMP/differs.txt"
          while read -r line; do
            case "$line" in
              Files\ *\ and\ *\ differ)
                a=${line#Files }; a=${a%% and *}
                b=${line#* and }; b=${b% differ}
                echo "::group::$b"
                echo "sizes: $(wc -c < "$a") vs $(wc -c < "$b")"
                off=$(cmp "$a" "$b" 2>/dev/null | sed -nE 's/.*byte ([0-9]+).*/\1/p')
                if [ -n "$off" ]; then
                  from=$(( off > 160 ? off - 160 : 0 ))
                  echo "first difference at byte $off; 320 bytes from $from:"
                  echo "--- first build"
                  dd if="$a" bs=1 skip="$from" count=320 status=none | cat -v
                  echo
                  echo "--- second build"
                  dd if="$b" bs=1 skip="$from" count=320 status=none | cat -v
                  echo
                fi
                echo "::endgroup::"
                ;;
            esac
          done < "$RUNNER_TEMP/differs.txt"
          echo "::error::The export is no longer reproducible. Something in the build now embeds a clock, a random id, or an absolute path — see the per-file byte context above. generateBuildId in next.config.mjs is the usual suspect."
          exit 1

      # A search index that exists but answers nothing is the classic silent
      # failure here — the page loads, the dialog opens, every query returns
      # empty, and no build step fails. A size floor is a crude check, but it
      # is the one that catches the corpus falling out of the index.
      - name: The search index is not empty
        run: |
          size=$(wc -c < out/api/search)
          echo "out/api/search is $size bytes"
          if [ "$size" -lt 50000 ]; then
            echo "::error::out/api/search is only $size bytes — the corpus is missing from the search index."
            exit 1
          fi
